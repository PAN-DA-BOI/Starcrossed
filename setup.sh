#!/bin/bash

apt-get update
apt-get upgrade -y


# setup screen

#setup GPOI for encoder

#install all necessary packages

#we need to decide if we're going to use python or C++ for this project. if we use C++ do we have them compile it locally? and if we compile locally, do we add a loading screeen?

echo "The system will now reboot."
sudo reboot now
